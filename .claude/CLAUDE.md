# claude-code-first — Claude Code

All project guidelines are in the shared agents file:

@../agents.md

<!-- Under .claude/ and not at the root: this repository will ship an audit plugin, and
every root file ships in a plugin payload, where a root CLAUDE.md is the one file the
loader warns is not context (roadkeep RK323). -->
